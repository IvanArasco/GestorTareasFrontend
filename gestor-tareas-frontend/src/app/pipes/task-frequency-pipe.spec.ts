import { TaskFrequencyPipe } from './task-frequency-pipe';

describe('TaskFrequencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskFrequencyPipe();
    expect(pipe).toBeTruthy();
  });
});
